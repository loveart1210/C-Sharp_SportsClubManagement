<UserControl x:Class="SportsClubManagement.Views.PersonalScheduleView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:vm="clr-namespace:SportsClubManagement.ViewModels"
             xmlns:conv="clr-namespace:SportsClubManagement.Converters"
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="900">
    <UserControl.DataContext>
        <vm:PersonalScheduleViewModel />
    </UserControl.DataContext>

    <UserControl.Resources>
        <conv:IntToVisibilityConverter x:Key="IntToVisibilityConverter"/>
    </UserControl.Resources>

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <DockPanel Grid.Row="0" Margin="0,0,0,15">
            <TextBlock Text="Lịch Tập Cá Nhân" FontSize="22" FontWeight="Bold" Foreground="#1976D2" VerticalAlignment="Center"/>
            
            <!-- Filters -->
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" VerticalAlignment="Center" 
                        Visibility="{Binding SelectedTabIndex, Converter={StaticResource IntToVisibilityConverter}, ConverterParameter=1}">
                <StackPanel Margin="10,0,0,0">
                    <TextBlock Text="Trạng thái" FontSize="11" Foreground="#333" Margin="0,0,0,2"/>
                    <ComboBox ItemsSource="{Binding StatusOptions}" SelectedItem="{Binding FilterStatus}" Width="100" Height="30" VerticalContentAlignment="Center"/>
                </StackPanel>
                <StackPanel Margin="5,0,0,0">
                    <TextBlock Text="Ngày" FontSize="11" Foreground="#333" Margin="0,0,0,2"/>
                    <ComboBox ItemsSource="{Binding DayOptions}" SelectedItem="{Binding FilterDay}" Width="60" Height="30" VerticalContentAlignment="Center"/>
                </StackPanel>
                <StackPanel Margin="5,0,0,0">
                    <TextBlock Text="Tháng" FontSize="11" Foreground="#333" Margin="0,0,0,2"/>
                    <ComboBox ItemsSource="{Binding MonthOptions}" SelectedItem="{Binding FilterMonth}" Width="60" Height="30" VerticalContentAlignment="Center"/>
                </StackPanel>
                <StackPanel Margin="5,0,0,0">
                    <TextBlock Text="Năm" FontSize="11" Foreground="#333" Margin="0,0,0,2"/>
                    <ComboBox ItemsSource="{Binding YearOptions}" SelectedItem="{Binding FilterYear}" Width="75" Height="30" VerticalContentAlignment="Center"/>
                </StackPanel>
                <StackPanel Margin="5,0,0,0">
                    <TextBlock Text="Môn" FontSize="11" Foreground="#333" Margin="0,0,0,2"/>
                    <ComboBox ItemsSource="{Binding SubjectNameOptions}" SelectedItem="{Binding FilterSubjectName}" Width="100" Height="30" VerticalContentAlignment="Center"/>
                </StackPanel>
                
                <Button Content="Xóa lọc" Command="{Binding ClearFiltersCommand}" Margin="10,8,0,0" 
                        Style="{StaticResource ModernButtonStyle}" Background="#9E9E9E" Foreground="White" Padding="12,4" VerticalAlignment="Center"/>
            </StackPanel>
        </DockPanel>

        <TabControl Grid.Row="1" SelectedIndex="{Binding SelectedTabIndex}">
            <!-- Subjects Tab -->
            <TabItem Header="Môn tập cá nhân">
                <Grid Margin="15">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="3*"/>
                        <ColumnDefinition Width="2*"/>
                    </Grid.ColumnDefinitions>

                    <!-- List Subjects -->
                    <DockPanel Grid.Column="0" Margin="0,0,20,0">
                        <TextBlock DockPanel.Dock="Top" Text="Danh sách môn tập" FontSize="18" FontWeight="Bold" Margin="0,0,0,15"/>
                        <Grid>
                            <DataGrid ItemsSource="{Binding Subjects}" SelectedItem="{Binding SelectedSubject}" 
                                      Style="{StaticResource ModernDataGridStyle}">
                                <DataGrid.Columns>
                                    <DataGridTemplateColumn Header="Môn tập" Width="*">
                                        <DataGridTemplateColumn.CellTemplate>
                                            <DataTemplate>
                                                <StackPanel VerticalAlignment="Center" HorizontalAlignment="Left" Margin="10,0">
                                                    <TextBlock Text="{Binding Name}" FontWeight="Bold" Foreground="#333" HorizontalAlignment="Left"/>
                                                    <TextBlock Text="{Binding Description}" FontSize="12" Foreground="#888" TextTrimming="CharacterEllipsis" MaxWidth="300" HorizontalAlignment="Left"/>
                                                </StackPanel>
                                            </DataTemplate>
                                        </DataGridTemplateColumn.CellTemplate>
                                    </DataGridTemplateColumn>

                                    <DataGridTemplateColumn Header="Thao tác" Width="Auto">
                                        <DataGridTemplateColumn.CellTemplate>
                                            <DataTemplate>
                                                <StackPanel Orientation="Horizontal">
                                                    <Button Content="Sửa" Style="{StaticResource ModernButtonStyle}"
                                                            Command="{Binding DataContext.EditSubjectCommand, RelativeSource={RelativeSource AncestorType=UserControl}}" 
                                                            CommandParameter="{Binding}"
                                                            Background="#E3F2FD" Foreground="#1976D2" Padding="8,4"/>
                                                    <Button Content="Xóa" Style="{StaticResource ModernButtonStyle}"
                                                            Command="{Binding DataContext.RemoveSubjectCommand, RelativeSource={RelativeSource AncestorType=UserControl}}" 
                                                            CommandParameter="{Binding}"
                                                            Background="#FFEBEE" Foreground="#C62828" Margin="5,0,0,0" Padding="8,4"/>
                                                </StackPanel>
                                            </DataTemplate>
                                        </DataGridTemplateColumn.CellTemplate>
                                    </DataGridTemplateColumn>
                                </DataGrid.Columns>
                            </DataGrid>
                        </Grid>
                    </DockPanel>

                    <!-- Create Subject -->
                    <Border Grid.Column="1" BorderBrush="#DDD" BorderThickness="1" Padding="15" VerticalAlignment="Top" Background="#F9F9F9">
                        <StackPanel>
                            <TextBlock Text="Thêm môn tập mới" FontSize="16" FontWeight="Bold" Margin="0,0,0,15"/>
                            
                            <TextBlock Text="Tên môn tập" FontWeight="SemiBold" Foreground="#333" Margin="0,0,0,5"/>
                            <Border BorderBrush="#CCCCCC" BorderThickness="1" Height="36" Margin="0,0,0,10" Background="White">
                                <TextBox Text="{Binding NewSubjectName, UpdateSourceTrigger=PropertyChanged}" 
                                         Style="{StaticResource SearchTextBoxStyle}" Tag="Nhập tên môn tập..."/>
                            </Border>
                            
                            <TextBlock Text="Mô tả" FontWeight="SemiBold" Foreground="#333" Margin="0,0,0,5"/>
                            <Border BorderBrush="#CCCCCC" BorderThickness="1" Margin="0,0,0,15" Background="White">
                                <TextBox Text="{Binding NewSubjectDesc}" AcceptsReturn="True" TextWrapping="Wrap"
                                         BorderThickness="0" Background="Transparent" Padding="8" MinHeight="80"/>
                            </Border>
                            
                            <Button Content="Thêm Môn Tập" Command="{Binding AddSubjectCommand}" 
                                    Style="{StaticResource ModernButtonStyle}" Background="#4CAF50" Foreground="White" Padding="12,8"/>
                        </StackPanel>
                    </Border>
                </Grid>
            </TabItem>

            <!-- Schedule/Calendar Tab -->
            <TabItem Header="Lịch tập cá nhân">
                <Grid Margin="15">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="280"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>

                    <!-- Sidebar Controls -->
                    <StackPanel Grid.Column="0" Margin="0,0,20,0">
                        <Border BorderBrush="#CCCCCC" BorderThickness="1" Margin="0,0,0,15" Padding="5" Background="White">
                            <Calendar SelectedDate="{Binding SelectedDate}" BorderThickness="0" Background="Transparent" HorizontalAlignment="Center"/>
                        </Border>
                        
                        <Border BorderBrush="#DDD" BorderThickness="1" Background="#F9F9F9" Padding="15">
                            <ScrollViewer VerticalScrollBarVisibility="Auto" MaxHeight="400">
                                <StackPanel>
                                    <TextBlock Text="Thêm buổi tập" FontSize="16" FontWeight="Bold" Foreground="#333" Margin="0,0,0,12"/>
                                    
                                    <TextBlock Text="Tên buổi tập" FontWeight="SemiBold" Foreground="#333" Margin="0,0,0,3"/>
                                    <Border BorderBrush="#CCCCCC" BorderThickness="1" Height="32" Margin="0,0,0,10" Background="White">
                                        <TextBox Text="{Binding NewSessionName, UpdateSourceTrigger=PropertyChanged}" 
                                                 Style="{StaticResource SearchTextBoxStyle}" Tag="VD: Tập luyện sáng..."/>
                                    </Border>
                                    
                                    <TextBlock Text="Môn tập" FontWeight="SemiBold" Foreground="#333" Margin="0,0,0,3"/>
                                    <ComboBox ItemsSource="{Binding Subjects}" SelectedItem="{Binding SelectedSubject}" 
                                              DisplayMemberPath="Name" Margin="0,0,0,10" Height="32" VerticalContentAlignment="Center"/>
                                    
                                    <Grid Margin="0,0,0,10">
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="*"/>
                                            <ColumnDefinition Width="10"/>
                                            <ColumnDefinition Width="*"/>
                                        </Grid.ColumnDefinitions>
                                        <StackPanel Grid.Column="0">
                                            <TextBlock Text="Bắt đầu" FontSize="11" Foreground="#333" Margin="0,0,0,2"/>
                                            <Border BorderBrush="#CCCCCC" BorderThickness="1" Height="32">
                                                <TextBox Text="{Binding NewSessionStart}" Style="{StaticResource SearchTextBoxStyle}" Tag="08:00"/>
                                            </Border>
                                        </StackPanel>
                                        <StackPanel Grid.Column="2">
                                            <TextBlock Text="Kết thúc" FontSize="11" Foreground="#333" Margin="0,0,0,2"/>
                                            <Border BorderBrush="#CCCCCC" BorderThickness="1" Height="32">
                                                <TextBox Text="{Binding NewSessionEnd}" Style="{StaticResource SearchTextBoxStyle}" Tag="10:00"/>
                                            </Border>
                                        </StackPanel>
                                    </Grid>
                                    
                                    <TextBlock Text="Ghi chú" FontWeight="SemiBold" Foreground="#333" Margin="0,0,0,3"/>
                                    <Border BorderBrush="#CCCCCC" BorderThickness="1" Margin="0,0,0,15" Background="White">
                                        <TextBox Text="{Binding NewSessionNote}" Padding="5" BorderThickness="0" Background="Transparent" MinHeight="40"/>
                                    </Border>
                                    
                                    <Button Content="Thêm Buổi Tập" Command="{Binding AddSessionCommand}" 
                                            Style="{StaticResource ModernButtonStyle}" Background="#2196F3" Foreground="White" 
                                            Height="36" Padding="12,0" Margin="0,0,0,10"/>

                                    <Button Content="Xuất Lịch (CSV)" Command="{Binding ExportScheduleCommand}"
                                            Style="{StaticResource ModernButtonStyle}" Background="#FF9800" Foreground="White" Height="36" Padding="12,0"/>
                                </StackPanel>
                            </ScrollViewer>
                        </Border>
                    </StackPanel>

                    <!-- Sessions List -->
                    <DockPanel Grid.Column="1">
                        <TextBlock DockPanel.Dock="Top" Text="{Binding ScheduleHeaderText}" 
                                   FontSize="18" FontWeight="Bold" Foreground="#1976D2" Margin="0,0,0,15"/>
                        
                        <Grid>
                            <DataGrid ItemsSource="{Binding SessionViewItems}" Style="{StaticResource ModernDataGridStyle}">
                                <DataGrid.Columns>
                                    <DataGridTemplateColumn Header="Thời gian" Width="100">
                                        <DataGridTemplateColumn.CellTemplate>
                                            <DataTemplate>
                                                <StackPanel VerticalAlignment="Center">
                                                    <TextBlock Text="{Binding Session.StartTime, StringFormat=dd/MM/yyyy}" FontWeight="SemiBold" Foreground="#333"/>
                                                    <TextBlock FontSize="11" Foreground="#888">
                                                        <TextBlock.Text>
                                                            <MultiBinding StringFormat="{}{0:HH:mm} - {1:HH:mm}">
                                                                <Binding Path="Session.StartTime" />
                                                                <Binding Path="Session.EndTime" />
                                                            </MultiBinding>
                                                        </TextBlock.Text>
                                                    </TextBlock>
                                                </StackPanel>
                                            </DataTemplate>
                                        </DataGridTemplateColumn.CellTemplate>
                                    </DataGridTemplateColumn>

                                    <DataGridTemplateColumn Header="Nội dung" Width="250">
                                        <DataGridTemplateColumn.CellTemplate>
                                            <DataTemplate>
                                                <StackPanel VerticalAlignment="Center" HorizontalAlignment="Left" Margin="10,0">
                                                    <TextBlock Text="{Binding Session.Name}" FontWeight="Bold" Foreground="#1976D2" HorizontalAlignment="Left"/>
                                                    <TextBlock Text="{Binding SubjectName}" FontSize="11" Foreground="#888" HorizontalAlignment="Left"/>
                                                </StackPanel>
                                            </DataTemplate>
                                        </DataGridTemplateColumn.CellTemplate>
                                    </DataGridTemplateColumn>

                                    <DataGridTemplateColumn Header="Ghi chú" Width="*">
                                        <DataGridTemplateColumn.CellTemplate>
                                            <DataTemplate>
                                                <TextBlock Text="{Binding Session.Note}" VerticalAlignment="Center" HorizontalAlignment="Left"
                                                           Foreground="#666" Margin="10,0" TextTrimming="CharacterEllipsis"/>
                                            </DataTemplate>
                                        </DataGridTemplateColumn.CellTemplate>
                                    </DataGridTemplateColumn>

                                    <DataGridTemplateColumn Header="Trạng thái" Width="130">
                                        <DataGridTemplateColumn.CellTemplate>
                                            <DataTemplate>
                                                <Border CornerRadius="10" Padding="8,2" HorizontalAlignment="Center" VerticalAlignment="Center" MinWidth="110" MinHeight="24">
                                                    <Border.Style>
                                                        <Style TargetType="Border">
                                                            <Setter Property="Background" Value="#F5F5F5"/>
                                                            <Style.Triggers>
                                                                <DataTrigger Binding="{Binding Status}" Value="Đang diễn ra">
                                                                    <Setter Property="Background" Value="#FFF3E0"/>
                                                                </DataTrigger>
                                                                <DataTrigger Binding="{Binding Status}" Value="Hoàn thành">
                                                                    <Setter Property="Background" Value="#E8F5E9"/>
                                                                </DataTrigger>
                                                                <DataTrigger Binding="{Binding Status}" Value="Quá hạn">
                                                                    <Setter Property="Background" Value="#FFEBEE"/>
                                                                </DataTrigger>
                                                            </Style.Triggers>
                                                        </Style>
                                                    </Border.Style>
                                                    <TextBlock Text="{Binding Status}" FontSize="11" FontWeight="Bold" HorizontalAlignment="Center" VerticalAlignment="Center">
                                                        <TextBlock.Style>
                                                            <Style TargetType="TextBlock">
                                                                <Setter Property="Foreground" Value="#666666"/>
                                                                <Style.Triggers>
                                                                    <DataTrigger Binding="{Binding Status}" Value="Đang diễn ra">
                                                                        <Setter Property="Foreground" Value="#E65100"/>
                                                                    </DataTrigger>
                                                                    <DataTrigger Binding="{Binding Status}" Value="Hoàn thành">
                                                                        <Setter Property="Foreground" Value="#2E7D32"/>
                                                                    </DataTrigger>
                                                                    <DataTrigger Binding="{Binding Status}" Value="Quá hạn">
                                                                        <Setter Property="Foreground" Value="#C62828"/>
                                                                    </DataTrigger>
                                                                </Style.Triggers>
                                                            </Style>
                                                        </TextBlock.Style>
                                                    </TextBlock>
                                                </Border>
                                            </DataTemplate>
                                        </DataGridTemplateColumn.CellTemplate>
                                    </DataGridTemplateColumn>

                                    <DataGridTemplateColumn Header="Thao tác" Width="Auto">
                                        <DataGridTemplateColumn.CellTemplate>
                                            <DataTemplate>
                                                <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                                                    <Button Content="Điểm danh" 
                                                            Command="{Binding DataContext.AttendSessionCommand, RelativeSource={RelativeSource AncestorType=UserControl}}" 
                                                            CommandParameter="{Binding}"
                                                            Style="{StaticResource ModernButtonStyle}"
                                                            Background="{Binding AttendanceColor}"
                                                            Foreground="White"
                                                            IsEnabled="{Binding IsAttendanceEnabled}"
                                                            Padding="8,4" FontSize="11"/>
                                                    
                                                    <Button Content="🗑️" Style="{StaticResource ModernButtonStyle}"
                                                            Command="{Binding DataContext.RemoveSessionCommand, RelativeSource={RelativeSource AncestorType=UserControl}}" 
                                                            CommandParameter="{Binding}"
                                                            Background="Transparent" Foreground="#C62828" FontSize="14" Margin="5,0,0,0" Padding="5"/>
                                                </StackPanel>
                                            </DataTemplate>
                                        </DataGridTemplateColumn.CellTemplate>
                                    </DataGridTemplateColumn>
                                </DataGrid.Columns>
                            </DataGrid>
                        </Grid>
                    </DockPanel>
                </Grid>
            </TabItem>
        </TabControl>
    </Grid>
</UserControl>
