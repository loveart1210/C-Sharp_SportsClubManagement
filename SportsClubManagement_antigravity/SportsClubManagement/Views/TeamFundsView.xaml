<UserControl x:Class="SportsClubManagement.Views.TeamFundsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Balance Info -->
        <Border Background="#4CAF50" Padding="20" Margin="0,0,0,20" CornerRadius="5">
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                <TextBlock Text="Total Balance:" Foreground="White" FontSize="20" FontWeight="Bold" Margin="0,0,10,0"/>
                <TextBlock Text="{Binding TotalBalance, StringFormat={}{0:N0} VND}" Foreground="White" FontSize="20"/>
            </StackPanel>
        </Border>

        <!-- Transaction Form -->
        <Border Grid.Row="1" BorderBrush="#DDD" BorderThickness="1" CornerRadius="5" Padding="15" Margin="0,0,0,20">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                
                <StackPanel Orientation="Horizontal" Grid.Column="0">
                    <RadioButton Content="Deposit" IsChecked="{Binding IsDeposit}" Margin="0,0,10,0" VerticalAlignment="Center"/>
                    <RadioButton Content="Withdraw" IsChecked="{Binding IsDeposit, Converter={StaticResource InverseBoolConverter}}" VerticalAlignment="Center"/> 
                    <!-- Note: Converter not implemented yet, simple toggle logic might be better via property or just using IsDeposit=False via RB Group -->
                    <!-- Re-impl simplified: Two RBs binding to same bool? WPF needs ValueConverter usually. Let's rely on View logic or simple UI for now. -->
                </StackPanel>
                
                <StackPanel Grid.Column="1" Orientation="Horizontal" HorizontalAlignment="Center">
                    <TextBlock Text="Amount:" VerticalAlignment="Center" Margin="0,0,5,0"/>
                    <TextBox Text="{Binding Amount}" Width="100" Margin="0,0,15,0" Padding="3"/>
                    
                    <TextBlock Text="Description:" VerticalAlignment="Center" Margin="0,0,5,0"/>
                    <TextBox Text="{Binding Description}" Width="200" Margin="0,0,15,0" Padding="3"/>
                </StackPanel>
                
                <Button Grid.Column="2" Content="Submit Transaction" Command="{Binding SubmitTransactionCommand}" Padding="15,5"/>
            </Grid>
        </Border>

        <!-- History -->
        <DataGrid Grid.Row="2" ItemsSource="{Binding Transactions}" AutoGenerateColumns="False" IsReadOnly="True">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Date" Binding="{Binding Date}" Width="150"/>
                <DataGridTextColumn Header="Description" Binding="{Binding Description}" Width="*"/>
                <DataGridTextColumn Header="Amount" Binding="{Binding Amount, StringFormat={}{0:N0}}" Width="100" FontWeight="Bold"/>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</UserControl>
