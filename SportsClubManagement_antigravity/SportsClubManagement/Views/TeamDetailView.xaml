<UserControl x:Class="SportsClubManagement.Views.TeamDetailView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:vm="clr-namespace:SportsClubManagement.ViewModels"
             xmlns:views="clr-namespace:SportsClubManagement.Views"
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="900">


    <UserControl.Resources>
        <DataTemplate DataType="{x:Type vm:TeamMembersViewModel}">
            <views:TeamMembersView/>
        </DataTemplate>
        <DataTemplate DataType="{x:Type vm:TeamSubjectsViewModel}">
            <views:TeamSubjectsView/>
        </DataTemplate>
        <DataTemplate DataType="{x:Type vm:TeamSessionsViewModel}">
            <views:TeamSessionsView/>
        </DataTemplate>
        <DataTemplate DataType="{x:Type vm:AttendanceViewModel}">
            <views:AttendanceView/>
        </DataTemplate>
        <DataTemplate DataType="{x:Type vm:TeamSettingsViewModel}">
            <views:TeamSettingsView/>
        </DataTemplate>
        <DataTemplate DataType="{x:Type vm:TeamNotificationsViewModel}">
            <views:TeamNotificationsView/>
        </DataTemplate>
        <DataTemplate DataType="{x:Type vm:TeamFundsViewModel}">
            <views:TeamFundsView/>
        </DataTemplate>
    </UserControl.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <Grid Grid.Row="0" Margin="0,0,0,20">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            

            
            <StackPanel Grid.Column="1" VerticalAlignment="Center">
                <TextBlock Text="{Binding Team.Name}" FontSize="24" FontWeight="Bold" Foreground="#1976D2"/>
                <TextBlock Text="{Binding Team.Description}" Foreground="#666" FontSize="12"/>
            </StackPanel>
            
            <Button Grid.Column="2" Content="← Quay lại" Command="{Binding BackCommand}" 
                    Padding="12,8" Cursor="Hand" Foreground="#666"/>
        </Grid>

        <!-- Tabs -->
        <TabControl Grid.Row="1" SelectedIndex="{Binding SelectedTabIndex, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                    SelectionChanged="TabControl_SelectionChanged">
            <!-- Tab 1: Members -->
            <TabItem Header="👥 Thành viên">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>
                    
                    <TextBlock Text="Danh sách thành viên" FontSize="14" FontWeight="Bold" Margin="20,15,0,10"/>
                    
                    <ContentControl Grid.Row="1" Content="{Binding MembersVM}" Margin="20,0,20,20"/>
                </Grid>
            </TabItem>

            <!-- Tab 2: Subjects -->
            <TabItem Header="📚 Môn tập">
                <ContentControl Content="{Binding SubjectsVM}"/>
            </TabItem>

            <!-- Tab 3: Sessions -->
            <TabItem Header="🏋️ Buổi tập">
                 <ContentControl Content="{Binding SessionsVM}"/>
            </TabItem>

            <!-- Tab 4: Attendance -->
            <TabItem Header="✅ Điểm danh">
                 <ContentControl Content="{Binding AttendanceVM}"/>
            </TabItem>

            <!-- Tab 5: Notifications -->
            <TabItem Header="🔔 Thông báo">
                <ContentControl Content="{Binding NotificationsVM}"/>
            </TabItem>

            <!-- Tab 6: Funds -->
            <TabItem Header="💰 Quỹ nhóm">
                <ContentControl Content="{Binding FundsVM}"/>
            </TabItem>

            <!-- Tab 7: Settings -->
            <TabItem Header="⚙️ Cài đặt">
                <ContentControl Content="{Binding SettingsVM}"/>
            </TabItem>
        </TabControl>
    </Grid>
</UserControl>
