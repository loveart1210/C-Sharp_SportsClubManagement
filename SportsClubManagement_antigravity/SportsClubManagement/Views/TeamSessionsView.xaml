<UserControl x:Class="SportsClubManagement.Views.TeamSessionsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="500" d:DesignWidth="800">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="300"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <!-- Left: Calendar -->
        <Border BorderBrush="#DDD" BorderThickness="0,0,1,0" Padding="10">
            <StackPanel>
                <TextBlock Text="Select Date" FontWeight="Bold" Margin="0,0,0,10"/>
                <Calendar SelectedDate="{Binding SelectedDate}" Margin="0,0,0,20"/>
                
                <TextBlock Text="Add New Session" FontWeight="Bold" Margin="0,0,0,10"/>
                <StackPanel>
                    <TextBlock Text="Session Name"/>
                    <TextBox Text="{Binding NewSessionName}" Margin="0,0,0,5" Padding="3"/>

                    <TextBlock Text="Subject"/>
                    <ComboBox ItemsSource="{Binding AvailableSubjects}" DisplayMemberPath="Name" SelectedItem="{Binding SelectedSubject}" Margin="0,0,0,5"/>

                    <TextBlock Text="Note"/>
                    <TextBox Text="{Binding NewSessionNote}" Margin="0,0,0,10" Padding="3"/>
                    
                    <Button Content="Add Session" Command="{Binding AddSessionCommand}" Padding="5"/>
                </StackPanel>
            </StackPanel>
        </Border>

        <!-- Right: Daily Schedule -->
        <Grid Grid.Column="1" Margin="10">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>
            
            <TextBlock Text="{Binding SelectedDate, StringFormat='Schedule for {0:D}'}" FontSize="18" FontWeight="Bold" Margin="0,0,0,10"/>
            
            <DataGrid Grid.Row="1" ItemsSource="{Binding Sessions}" AutoGenerateColumns="False" IsReadOnly="True">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="Time" Binding="{Binding StartTime, StringFormat=HH:mm}" Width="60"/>
                    <DataGridTextColumn Header="Name" Binding="{Binding Name}" Width="150"/>
                    <DataGridTextColumn Header="Note" Binding="{Binding Note}" Width="*"/>
                    <DataGridTemplateColumn Header="Action" Width="80">
                         <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <Button Content="Del" Command="{Binding DataContext.RemoveSessionCommand, RelativeSource={RelativeSource AncestorType=UserControl}}" 
                                    CommandParameter="{Binding}" Background="#FFD4D4"/>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>
                </DataGrid.Columns>
            </DataGrid>
        </Grid>
    </Grid>
</UserControl>
