<UserControl x:Class="SportsClubManagement.Views.TeamSettingsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:vm="clr-namespace:SportsClubManagement.ViewModels"
             xmlns:conv="clr-namespace:SportsClubManagement.Converters"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    
    <UserControl.Resources>
        <conv:BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter"/>
        <conv:InverseBooleanToVisibilityConverter x:Key="InverseBooleanToVisibilityConverter"/>
    </UserControl.Resources>

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Cài đặt Nhóm" FontSize="20" FontWeight="Bold" Foreground="#1976D2" Margin="0,0,0,10"/>
        
        <!-- Invite Code Section -->
        <Border Grid.Row="1" Background="White" BorderBrush="#E0E0E0" BorderThickness="1" CornerRadius="8" Padding="15" Margin="0,0,0,20">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <StackPanel>
                    <TextBlock Text="Mã tham gia nhóm" FontSize="14" Foreground="#666" Margin="0,0,0,5"/>
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="{Binding JoinCode}" FontSize="28" FontWeight="Bold" Foreground="#1976D2"/>
                        <Button Content="📋 Copy" Margin="15,0,0,0" VerticalAlignment="Center" 
                                Padding="8,4" Background="Transparent" BorderBrush="#DDD" Cursor="Hand"
                                Click="CopyCode_Click"/>
                    </StackPanel>
                    <TextBlock Text="Gửi mã này cho những người bạn muốn mời vào nhóm." FontSize="12" Foreground="Gray" Margin="0,5,0,0"/>
                </StackPanel>
                <Button Grid.Column="1" Content="🔄 Tạo mã mới" Command="{Binding RegenerateCodeCommand}"
                        VerticalAlignment="Center" Padding="12,8" Background="White" BorderBrush="#FBC02D" 
                        Foreground="#F9A825" FontWeight="SemiBold" Cursor="Hand"/>
            </Grid>
        </Border>

        <Border Grid.Row="2" Background="#FFF9C4" BorderBrush="#FBC02D" BorderThickness="1" Padding="15" CornerRadius="4" Margin="0,0,0,20">
            <StackPanel Orientation="Horizontal">
                <TextBlock Text="⚠️" FontSize="18" Margin="0,0,10,0" VerticalAlignment="Center"/>
                <TextBlock Text="Khu vực dành cho Quản trị viên. Bạn có thể thay đổi vai trò hoặc mời/xóa thành viên." VerticalAlignment="Center" Foreground="#5D4037"/>
            </StackPanel>
        </Border>

        <Grid Grid.Row="2">
            <ListBox ItemsSource="{Binding Members}" BorderThickness="0" HorizontalContentAlignment="Stretch" ScrollViewer.HorizontalScrollBarVisibility="Disabled">
                <ListBox.ItemTemplate>
                    <DataTemplate>
                        <Border BorderBrush="#EEEEEE" BorderThickness="0,0,0,1" Padding="10,15">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>

                                <StackPanel Grid.Column="0">
                                    <StackPanel Orientation="Horizontal">
                                        <TextBlock Text="{Binding FullName}" FontWeight="Bold" FontSize="15"/>
                                        <Border Background="#E3F2FD" CornerRadius="10" Padding="8,2" Margin="10,0,0,0" VerticalAlignment="Center">
                                            <TextBlock Text="{Binding Role}" FontSize="11" Foreground="#1976D2" FontWeight="SemiBold"/>
                                        </Border>
                                        <TextBlock Text="(Bạn)" FontSize="12" Foreground="Gray" Margin="5,0,0,0" VerticalAlignment="Center" 
                                                   Visibility="{Binding IsCurrentUser, Converter={StaticResource BooleanToVisibilityConverter}}"/>
                                    </StackPanel>
                                    <TextBlock Text="{Binding Email}" Foreground="Gray" FontSize="13" Margin="0,5,0,0"/>
                                    <TextBlock Text="{Binding JoinDate, StringFormat='Tham gia: {0:dd/MM/yyyy}'}" Foreground="#999" FontSize="11" Margin="0,3,0,0"/>
                                </StackPanel>

                                <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Center">
                                    <Button Content="Đổi vai trò" 
                                            Command="{Binding DataContext.ChangeRoleCommand, RelativeSource={RelativeSource AncestorType=ListBox}}"
                                            CommandParameter="{Binding}"
                                            Visibility="{Binding CanChangeRole, Converter={StaticResource BooleanToVisibilityConverter}}"
                                            Padding="10,5" Margin="0,0,10,0" Background="White" BorderBrush="#CCC" Cursor="Hand"/>
                                    
                                    <Button Content="Xóa" 
                                            Command="{Binding DataContext.KickMemberCommand, RelativeSource={RelativeSource AncestorType=ListBox}}"
                                            CommandParameter="{Binding}"
                                            Visibility="{Binding CanBeKicked, Converter={StaticResource BooleanToVisibilityConverter}}"
                                            Padding="10,5" Background="#FFEBEE" Foreground="#D32F2F" BorderBrush="#FFCDD2" Cursor="Hand"/>
                                </StackPanel>
                            </Grid>
                        </Border>
                    </DataTemplate>
                </ListBox.ItemTemplate>
            </ListBox>
        </Grid>

        <!-- No Permission Overlay -->
        <Border Grid.Row="0" Grid.RowSpan="3" Background="#F5F5F5" 
                Visibility="{Binding CanManage, Converter={StaticResource InverseBooleanToVisibilityConverter}}" 
                Opacity="0.9">
            <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
                <TextBlock Text="🔒" FontSize="48" HorizontalAlignment="Center"/>
                <TextBlock Text="Bạn không có quyền truy cập phần này." FontSize="16" Margin="0,15,0,0" Foreground="#666"/>
                <TextBlock Text="Chỉ Founder hoặc Admin mới có thể quản lý nhóm." FontSize="14" Foreground="#999" HorizontalAlignment="Center"/>
            </StackPanel>
        </Border>
    </Grid>
</UserControl>
