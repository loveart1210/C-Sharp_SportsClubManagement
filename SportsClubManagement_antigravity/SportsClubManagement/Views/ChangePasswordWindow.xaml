<Window x:Class="SportsClubManagement.Views.ChangePasswordWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Đổi mật khẩu bắt buộc" Height="450" Width="400" 
        WindowStartupLocation="CenterScreen" ResizeMode="NoResize"
        SizeToContent="Height"
        Background="#F5F5F5">
    <Grid Margin="30">
        <StackPanel VerticalAlignment="Center">
            <TextBlock Text="🔒 Đổi mật khẩu" FontSize="24" FontWeight="Bold" Foreground="#1976D2" HorizontalAlignment="Center" Margin="0,0,0,10"/>
            <TextBlock Text="Tài khoản của bạn vừa được reset. Vui lòng nhập mật khẩu mới trước khi tiếp tục." 
                       TextWrapping="Wrap" TextAlignment="Center" Foreground="#666" Margin="0,0,0,20"/>

            <TextBlock Text="Mật khẩu mới (ít nhất 6 ký tự):" Margin="0,0,0,5" Foreground="#333"/>
            <PasswordBox x:Name="NewPasswordBox" Height="35" Margin="0,0,0,15" VerticalContentAlignment="Center" Padding="10,0"/>

            <TextBlock Text="Xác nhận mật khẩu:" Margin="0,0,0,5" Foreground="#333"/>
            <PasswordBox x:Name="ConfirmPasswordBox" Height="35" Margin="0,0,0,10" VerticalContentAlignment="Center" Padding="10,0"/>

            <TextBlock Text="{Binding ErrorMessage}" Foreground="#D32F2F" FontSize="12" Margin="0,0,0,15" 
                       HorizontalAlignment="Center" TextWrapping="Wrap"/>

            <Button Content="Cập nhật mật khẩu" Command="{Binding SaveCommand}" 
                    Height="40" Cursor="Hand">
                <Button.Style>
                    <Style TargetType="Button">
                        <Setter Property="Background" Value="#1976D2"/>
                        <Setter Property="Foreground" Value="White"/>
                        <Setter Property="FontWeight" Value="Bold"/>
                        <Setter Property="Padding" Value="10,5"/>
                        <Setter Property="Template">
                            <Setter.Value>
                                <ControlTemplate TargetType="Button">
                                    <Border Background="{TemplateBinding Background}" 
                                            CornerRadius="4" 
                                            BorderThickness="0">
                                        <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                    </Border>
                                </ControlTemplate>
                            </Setter.Value>
                        </Setter>
                        <Style.Triggers>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Background" Value="#EEEEEE"/>
                                <Setter Property="Foreground" Value="#888888"/>
                            </Trigger>
                        </Style.Triggers>
                    </Style>
                </Button.Style>
            </Button>
        </StackPanel>
    </Grid>
</Window>
