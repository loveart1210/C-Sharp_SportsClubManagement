<UserControl x:Class="SportsClubManagement.Views.TeamSubjectsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Search and Filters -->
        <StackPanel Orientation="Horizontal" Margin="0,0,0,10">
            <TextBox Width="200" Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}" Padding="5" Tag="Search subjects..." Margin="0,0,10,0"/>
            <ComboBox ItemsSource="{Binding AvailableMembers}" DisplayMemberPath="FullName" SelectedValuePath="UserId" SelectedValue="{Binding FilterMember}" Width="150" Margin="0,0,10,0" ToolTip="Filter by Member"/>
            <ComboBox ItemsSource="{Binding AvailableSessions}" DisplayMemberPath="Name" SelectedValuePath="Id" SelectedValue="{Binding FilterSession}" Width="150" ToolTip="Filter by Session"/>
        </StackPanel>

        <Border Grid.Row="1" BorderBrush="#DDD" BorderThickness="1" CornerRadius="5" Padding="10" Margin="0,0,0,10">
            <StackPanel Orientation="Vertical">
                <TextBlock Text="Add New Subject" FontWeight="Bold" Margin="0,0,0,5"/>
                <StackPanel Orientation="Horizontal">
                    <TextBox Width="150" Text="{Binding NewSubjectName, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,10,0" ToolTip="Subject Name"/>
                    <TextBox Width="300" Text="{Binding NewSubjectDesc, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,10,0" ToolTip="Description"/>
                    <Button Content="Add Subject" Command="{Binding AddSubjectCommand}" Padding="10,5"/>
                </StackPanel>
            </StackPanel>
        </Border>

        <DataGrid Grid.Row="2" ItemsSource="{Binding Subjects}" AutoGenerateColumns="False" IsReadOnly="True">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Subject Name" Binding="{Binding Name}" Width="200"/>
                <DataGridTextColumn Header="Description" Binding="{Binding Description}" Width="*"/>
                <DataGridTemplateColumn Header="Actions" Width="100">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <Button Content="Delete" Command="{Binding DataContext.RemoveSubjectCommand, RelativeSource={RelativeSource AncestorType=UserControl}}" 
                                    CommandParameter="{Binding}" Background="#FFD4D4"/>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</UserControl>
