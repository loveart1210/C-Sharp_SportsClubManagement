<UserControl x:Class="SportsClubManagement.Views.TeamMembersView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel Orientation="Horizontal" Margin="0,0,0,10">
            <TextBox Width="200" Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}" Padding="5" Tag="Search members..."/>
            <!-- Placeholder for watermark -->
            <!-- Filters -->
            <ComboBox ItemsSource="{Binding Roles}" SelectedItem="{Binding FilterRole}" Width="100" Margin="10,0,0,0" ToolTip="Filter by Role"/>
            <ComboBox ItemsSource="{Binding AvailableSubjects}" DisplayMemberPath="Name" SelectedValuePath="Id" SelectedValue="{Binding FilterSubject}" Width="150" Margin="10,0,0,0" ToolTip="Filter by Subject"/>
            <ComboBox ItemsSource="{Binding AvailableSessions}" DisplayMemberPath="Name" SelectedValuePath="Id" SelectedValue="{Binding FilterSession}" Width="150" Margin="10,0,0,0" ToolTip="Filter by Session"/>
            
            <Button Content="Add Member" Command="{Binding AddMemberCommand}" Margin="10,0,0,0" Padding="10,5"/>
        </StackPanel>

        <DataGrid Grid.Row="1" ItemsSource="{Binding Members}" AutoGenerateColumns="False" IsReadOnly="True">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Name" Binding="{Binding Name}" Width="*"/>
                <DataGridTextColumn Header="Role" Binding="{Binding Role}" Width="100"/>
                <DataGridTextColumn Header="Email" Binding="{Binding Email}" Width="150"/>
                <DataGridTextColumn Header="Joined" Binding="{Binding JoinDate}" Width="100"/>
                <DataGridTemplateColumn Header="Actions" Width="100">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <Button Content="Remove" Command="{Binding DataContext.RemoveMemberCommand, RelativeSource={RelativeSource AncestorType=UserControl}}" 
                                    CommandParameter="{Binding}" Background="#FFD4D4"/>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</UserControl>
