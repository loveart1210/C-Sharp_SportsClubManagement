<UserControl x:Class="SportsClubManagement.Views.AttendanceView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="600" d:DesignWidth="1000"
             Loaded="UserControl_Loaded">
    <UserControl.Resources>
        <BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter"/>
    </UserControl.Resources>

    <Grid Margin="25">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="320"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <!-- Left Sidebar: Selection & Stats -->
        <DockPanel Grid.Column="0" Margin="0,0,20,0">
            <!-- Calendar Section -->
            <Border DockPanel.Dock="Top" BorderBrush="#CCCCCC" BorderThickness="1" Margin="0,0,0,15" Padding="5" Background="White">
                <Calendar SelectedDate="{Binding SelectedDate}" BorderThickness="0" Background="Transparent" HorizontalAlignment="Center"/>
            </Border>
            
            <!-- Selection Section -->
            <Border BorderBrush="#DDD" BorderThickness="1" Padding="15" VerticalAlignment="Top" Background="#F9F9F9">
                <StackPanel>
                    <TextBlock Text="Điểm danh Buổi tập" FontSize="18" FontWeight="Bold" Foreground="#333" Margin="0,0,0,10"/>
                    
                    <TextBlock Text="Chọn Buổi Tập" FontWeight="SemiBold" Foreground="#333" Margin="0,0,0,5"/>
                    <ComboBox ItemsSource="{Binding Sessions}" SelectedItem="{Binding SelectedSession}" 
                              Margin="0,0,0,15" Height="36" VerticalContentAlignment="Center">
                        <ComboBox.ItemTemplate>
                            <DataTemplate>
                                <StackPanel Orientation="Horizontal">
                                    <TextBlock Text="{Binding Name}" FontWeight="Bold" Foreground="#1976D2"/>
                                    <TextBlock Text="{Binding StartTime, StringFormat=' ({0:HH:mm})'}" Foreground="#666"/>
                                </StackPanel>
                            </DataTemplate>
                        </ComboBox.ItemTemplate>
                    </ComboBox>
                    
                    <Border Background="#F1F3F4" CornerRadius="4" Padding="12" Margin="0,0,0,15">
                        <StackPanel>
                            <TextBlock Text="Thống kê nhanh" FontWeight="Bold" Foreground="#333" Margin="0,0,0,5"/>
                            <TextBlock Text="{Binding AttendanceSummary}" Foreground="#666" FontStyle="Italic" TextWrapping="Wrap" FontSize="12"/>
                        </StackPanel>
                    </Border>
 
                    <Button Content="Làm mới" Click="Button_Click" 
                            Style="{StaticResource ModernButtonStyle}" Background="#9E9E9E" Foreground="White" Padding="12,6"/>
                </StackPanel>
            </Border>
        </DockPanel>

        <!-- Main Content: Member List -->
        <DockPanel Grid.Column="1">
            <TextBlock DockPanel.Dock="Top" Text="{Binding HeaderText}" 
                       FontSize="24" FontWeight="Bold" Foreground="#1976D2" Margin="0,0,0,20"/>
            
            <Grid>
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>

                    <DataGrid ItemsSource="{Binding AttendanceRecords}" Style="{StaticResource ModernDataGridStyle}">
                        <DataGrid.Columns>
                            <DataGridTemplateColumn Header="Thành viên" Width="*">
                                <DataGridTemplateColumn.CellTemplate>
                                    <DataTemplate>
                                        <StackPanel Orientation="Horizontal" VerticalAlignment="Center" Margin="5">
                                            <Border Width="32" Height="32" CornerRadius="16" Background="#1976D2" Margin="0,0,10,0">
                                                <TextBlock Text="{Binding UserName[0]}" Foreground="White" 
                                                           HorizontalAlignment="Center" VerticalAlignment="Center" FontWeight="Bold"/>
                                            </Border>
                                            <StackPanel VerticalAlignment="Center">
                                                <TextBlock Text="{Binding UserName}" FontWeight="Bold" Foreground="#333"/>
                                                <TextBlock Text="{Binding Role}" FontSize="11" Foreground="#888"/>
                                            </StackPanel>
                                        </StackPanel>
                                    </DataTemplate>
                                </DataGridTemplateColumn.CellTemplate>
                            </DataGridTemplateColumn>

                            <DataGridTemplateColumn Header="Có mặt" Width="100">
                                <DataGridTemplateColumn.CellTemplate>
                                    <DataTemplate>
                                        <CheckBox IsChecked="{Binding IsPresent, UpdateSourceTrigger=PropertyChanged}" 
                                                  HorizontalAlignment="Center" VerticalAlignment="Center"
                                                  IsEnabled="{Binding DataContext.IsFounderOrCoach, RelativeSource={RelativeSource AncestorType=UserControl}}"/>
                                    </DataTemplate>
                                </DataGridTemplateColumn.CellTemplate>
                            </DataGridTemplateColumn>

                            <DataGridTemplateColumn Header="Ghi chú" Width="*">
                                <DataGridTemplateColumn.CellTemplate>
                                    <DataTemplate>
                                        <TextBox Text="{Binding Note, UpdateSourceTrigger=PropertyChanged}" 
                                                 Style="{StaticResource SearchTextBoxStyle}" Tag="Thêm ghi chú..."
                                                 IsEnabled="{Binding DataContext.IsFounderOrCoach, RelativeSource={RelativeSource AncestorType=UserControl}}"/>
                                    </DataTemplate>
                                </DataGridTemplateColumn.CellTemplate>
                            </DataGridTemplateColumn>
                        </DataGrid.Columns>
                    </DataGrid>

                    <!-- Save Button Area -->
                    <Border Grid.Row="1" Background="#F1F3F4" Padding="12" BorderBrush="#E0E0E0" BorderThickness="0,1,0,0"
                            Visibility="{Binding IsFounderOrCoach, Converter={StaticResource BooleanToVisibilityConverter}}">
                        <Button Content="Lưu Điểm Danh" Command="{Binding SaveAttendanceCommand}" 
                                Style="{StaticResource ModernButtonStyle}" Background="#4CAF50" Foreground="White" HorizontalAlignment="Right" Width="150" Height="36"/>
                    </Border>
                </Grid>
            </Grid>
        </DockPanel>
    </Grid>
</UserControl>
