<UserControl x:Class="SportsClubManagement.Views.AttendanceView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="600" d:DesignWidth="900">
    <Grid Background="White">
        <StackPanel Margin="20">
            <!-- Header -->
            <TextBlock Text="Quản lý Điểm Danh" FontSize="24" FontWeight="Bold" Margin="0,0,0,20"/>
            
            <!-- Filter Section -->
            <Border Background="#F5F5F5" Padding="15" Margin="0,0,0,20" CornerRadius="5">
                <StackPanel>
                    <TextBlock Text="Lọc" FontWeight="Bold" Margin="0,0,0,10"/>
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        
                        <StackPanel Grid.Column="0" Margin="0,0,10,0">
                            <TextBlock Text="Chọn Ngày" FontSize="12" Margin="0,0,0,5"/>
                            <DatePicker SelectedDate="{Binding SelectedDate, Mode=TwoWay}" Padding="8"/>
                        </StackPanel>
                        
                        <StackPanel Grid.Column="1" Margin="10,0,0,0">
                            <TextBlock Text="Chọn Buổi Tập" FontSize="12" Margin="0,0,0,5"/>
                            <ComboBox ItemsSource="{Binding Sessions}" SelectedItem="{Binding SelectedSession, Mode=TwoWay}"
                                     DisplayMemberPath="Name" Padding="8"/>
                        </StackPanel>
                        
                        <StackPanel Grid.Column="2" Margin="10,0,0,0" VerticalAlignment="Bottom">
                            <Button Command="{Binding MarkAllPresentCommand}" Content="✓ Tất Cả Có Mặt" 
                                   Background="#28A745" Foreground="White" Padding="15,8" Margin="0,0,5,0"/>
                            <Button Command="{Binding MarkAllAbsentCommand}" Content="✗ Tất Cả Vắng" 
                                   Background="#DC3545" Foreground="White" Padding="15,8"/>
                        </StackPanel>
                    </Grid>
                </StackPanel>
            </Border>
            
            <!-- Attendance Grid -->
            <DataGrid ItemsSource="{Binding AttendanceRecords}" AutoGenerateColumns="False" 
                     CanUserAddRows="False" CanUserDeleteRows="False" Margin="0,0,0,20" Height="350">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="Tên Thành Viên" Binding="{Binding UserName}" Width="200" IsReadOnly="True"/>
                    <DataGridCheckBoxColumn Header="Có Mặt" Binding="{Binding IsPresent, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" Width="100"/>
                    <DataGridTextColumn Header="Ghi Chú" Binding="{Binding Note, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" Width="*"/>
                </DataGrid.Columns>
            </DataGrid>
            
            <!-- Message -->
            <TextBlock Text="{Binding Message}" Foreground="Green" FontSize="12" Margin="0,0,0,10"/>
            
            <!-- Buttons -->
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                <Button Command="{Binding SaveAttendanceCommand}" Content="Lưu Điểm Danh" 
                       Background="#007BFF" Foreground="White" Padding="15,10" Margin="0,0,10,0"/>
            </StackPanel>
        </StackPanel>
    </Grid>
</UserControl>
